// Test 106: Collections — Set

import "std/collections"

fn main() {
    // Test 1: Basic set operations
    let s = set_new()
    set_add(s, "apple")
    set_add(s, "banana")
    set_add(s, "cherry")

    print(set_has(s, "apple"))
    assert(set_has(s, "apple") == true)
    print(set_has(s, "grape"))
    assert(set_has(s, "grape") == false)
    print(int_to_string(set_size(s)))
    assert(set_size(s) == 3)

    // Test 2: Duplicate add
    set_add(s, "apple")
    print(int_to_string(set_size(s)))
    assert(set_size(s) == 3)

    // Test 3: Remove
    set_remove(s, "banana")
    print(set_has(s, "banana"))
    assert(set_has(s, "banana") == false)
    print(int_to_string(set_size(s)))
    assert(set_size(s) == 2)

    // Test 4: set_to_array
    let s2 = set_new()
    set_add(s2, "x")
    set_add(s2, "y")
    let arr: Array = set_to_array(s2)
    print(int_to_string(arr.length()))
    assert(arr.length() == 2)

    // Test 5: Set union
    let a = set_new()
    set_add(a, "1")
    set_add(a, "2")
    let b = set_new()
    set_add(b, "2")
    set_add(b, "3")
    let u = set_union(a, b)
    print(int_to_string(set_size(u)))
    assert(set_size(u) == 3)

    // Test 6: Set intersection
    let inter = set_intersection(a, b)
    print(int_to_string(set_size(inter)))
    assert(set_size(inter) == 1)

    // Test 7: Integer set
    let si = set_new()
    set_add_int(si, 42)
    set_add_int(si, 99)
    print(set_has_int(si, 42))
    assert(set_has_int(si, 42) == true)
    print(set_has_int(si, 100))
    assert(set_has_int(si, 100) == false)

    print("Collection tests passed!")
}
