// Aviso de transición NYX2010 (fricción nyxerp 20260924-010014-team-1): con la
// atribución corregida `etiqueta` existe en dos módulos y la llamada es
// ambigua; hasta 0.33.x la de aux_ambig_x era del principal y ganaba. Sale
// como AVISO y el codegen resuelve como entonces (resolve_module_fn_legado):
// imprime "x".
import "tests/support/aux_ambig_x"
import "tests/support/aux_ambig_y"

fn main() -> int {
    print(etiqueta())
    return 0
}
