// M-08 sesion 3 error test: bitwise operator with non-integer operand.
// Expected stderr (contains): "left operand of '&' must be an integer, got String"
// Expected exit: non-zero (semantic check fails)
fn main() -> int {
    let s: String = "hola"
    let bad: int = s & 1
    return 0
}
