// A8: trim() es 0-arg — pasar un arg debe ser error, no ignorarse.
// Expected stderr (contains): "'trim' accepts at most 0 argument(s), got 1"
fn main() -> int {
    let s: String = "  hola  "
    let r: String = s.trim(42)
    return 0
}
